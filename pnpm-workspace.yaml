packages:
  - 'packages/*'
  - 'apps/*'

# pnpm blocks dependency build scripts by default. esbuild (pulled in by Vite
# and tsx) needs its own to place the platform binary.
allowBuilds:
  esbuild: true
